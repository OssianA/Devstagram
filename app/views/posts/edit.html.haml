.text-center
  = image_tag @post.image.url(:medium)
= render 'form'
.text-center
  = link_to "Delete Post", post_path(@post), method: :delete, data: { confirm: "Are you sure you want to delete the post?"}
  |
  =link_to "Cancel", posts_path